[package]
name = "esp32-battery-logic"
version = "0.1.0"
edition = "2024"

[dependencies]
heapless = "*"
log = "*"
strum = { version = "*", default-features = false, features = ["derive"] }
xy-modbus = { git = "https://github.com/xorza/xy-modbus", branch = "master", default-features = false }
