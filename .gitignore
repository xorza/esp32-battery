.DS_Store
.claude/scheduled_tasks.lock
/logs/*.log
/logs/*.txt
/logs/*.json

**/target
**/.embuild
**/.env
/certs
/.zed/settings.json

monitoring/volumes/
.tmp/

# Library crate built only via `-p` from the root, which uses the root
# lockfile. A second lockfile here is never consulted and silently drifts.
logic/Cargo.lock
